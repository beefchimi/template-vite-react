// I more than likely don't actually need this `definitions` file.
declare module '*.mp3' {
  const audio: string;
  export default audio;
}

declare module '*.webm' {
  const audio: string;
  export default audio;
}
